## 1. Contract validation

- [ ] 1.1 Validate the change with `openspec validate add-browser-rendering-contract --strict` from the workspace root. No failing test: contract documentation; the strict validator is the check.
- [ ] 1.2 Review the subject vocabulary against existing store specs (`cmd.*`/`evt.*` patterns, BlobRef media type) and commit. No failing test: consistency review.
