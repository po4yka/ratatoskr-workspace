## 1. Fleet preparation

- [x] 1.1 Fetch all 16 child remotes, create isolated `.workspaces/refresh-fleet-documentation/repos/<repo-id>/` worktrees from the observed remote `main` SHAs, record bases and one-writer ownership, and verify the existing shared dirty worktrees remain byte-for-byte unchanged; this is Git coordination and cannot start from a failing product test
- [x] 1.2 Inventory every tracked first-party Markdown file, deploy README, accepted ADR index, completed OpenSpec delta, and descriptive `openspec/config.yaml` block against source, manifests, CI, and recent history; record a no-finding result for reviewed files that need no edit, because documentation audit has no behavioral failing test

## 2. Implementation by repository

The user explicitly requires scoped commits directly on `main`, so these items record remote commit SHAs instead of pull-request links. Each item is documentation-only and cannot start from a failing behavioral test.

- [x] 2.1 `ratatoskr-contracts`: correct crate/contract inventory, gate description, and milestone ordering; run its full `DEVELOPMENT.md` gate and strict OpenSpec validation, commit only intended documentation paths to `main`, push, wait for workflows, and record the remote SHA (`3eecb5427f18c0bed35f1d95999fdcf6e035e312`)
- [x] 2.2 `ratatoskr-platform`: correct milestone status, capture request, event roles, metrics, and any other verified current-state drift while preserving user-owned `AGENTS.md`; run its full `DEVELOPMENT.md` gate and strict OpenSpec validation, commit to `main`, push, wait for workflows, and record the remote SHA (`560dd3a278354a0998fb07279c31d71746c7b9f2`)
- [x] 2.3 `ratatoskr-extractor`: document direct PDF implementation, current component and metric inventory, and implemented-versus-planned boundary; run its full `DEVELOPMENT.md` gate and strict OpenSpec validation, commit to `main`, push, wait for workflows, and record the remote SHA (`4bfedae6ab2370b002b36c4f2fd07b3d3d7b4a6f`)
- [x] 2.4 `ratatoskr-knowledge`: document implemented article analysis, provider support, credential behavior, and current OpenSpec capabilities; synchronize confirmed completed deltas without inventing behavior, run its full `DEVELOPMENT.md` gate and strict OpenSpec validation, commit to `main`, push, wait for workflows, and record the remote SHA (`ec8f875b45d9e4985d41c2fa6099cccf7dd9113d`)
- [x] 2.5 `ratatoskr-github`: repeat the audit on the current remote service scaffold, correct only confirmed drift, run its current full `DEVELOPMENT.md` gate and strict OpenSpec validation, commit to `main`, push, wait for workflows, and record the remote SHA (`1843e6bf48ca9a7d7527898657c6ea6663303971`)
- [x] 2.6 `ratatoskr-vault`: repeat the audit on the current remote service scaffold while preserving the shared `.omc/`, correct only confirmed drift, run its current full `DEVELOPMENT.md` gate and strict OpenSpec validation, commit to `main`, push, wait for workflows, and record the remote SHA (`e87a6a7ed4d6e11aba3287a917717781bd570e47`)
- [x] 2.7 `ratatoskr-telegram`: correct code-bearing context, durable work authority, implemented/planned flows, milestone split, schema wording, workspace integration status, and current recovery specs; run its full `DEVELOPMENT.md` gate and strict OpenSpec validation, commit to `main`, push, wait for workflows, and record the remote SHA (`916665bdab8e6fafec91160e9d46efc3b79bd2dc`)
- [x] 2.8 `ratatoskr-x`: correct future-schema, no-database-migration, current docs-only gate, and planned workspace integration wording while preserving shared `.omc/`; run both strict OpenSpec checks and available invariant checks, commit to `main`, push, wait for workflows, and record the remote SHA (`3ab877dc83a034e74fe26fe6b5dff5777b3cab80`)
- [x] 2.9 `ratatoskr-instagram`: correct future-schema, no-database-migration, current docs-only gate, and planned workspace integration wording; run both strict OpenSpec checks and available invariant checks, commit to `main`, push, wait for workflows, and record the remote SHA (`fcff7e2a22401c39a08da786abbbb56be6121619`)
- [x] 2.10 `ratatoskr-threads`: correct future-schema, no-database-migration, current docs-only gate, and planned workspace integration wording; run both strict OpenSpec checks and available invariant checks, commit to `main`, push, wait for workflows, and record the remote SHA (`61dbd37b2ed0be074d9474573ad42a6a5ad2ab85`)
- [x] 2.11 `ratatoskr-chatgpt`: correct future-schema, no-database-migration, current docs-only gate, and planned workspace integration wording; run both strict OpenSpec checks and available invariant checks, commit to `main`, push, wait for workflows, and record the remote SHA (`d78de0e2f990b862c1caf32329fe36508f55b3d0`)
- [x] 2.12 `ratatoskr-claude`: correct future-schema, no-database-migration, current docs-only gate, and planned workspace integration wording; run both strict OpenSpec checks and available invariant checks, commit to `main`, push, wait for workflows, and record the remote SHA (`33bf5865ab871d5a343c764dd157e92af3e17bbe`)
- [x] 2.13 `ratatoskr-web`: mark absent workspace pin and Compose E2E as planned, correct any additional verified current drift, run its full `DEVELOPMENT.md` gate and strict OpenSpec validation, commit to `main`, push, wait for workflows, and record the remote SHA (`16f31f7515dc8776f053f9cd55b30dbf3a3585e2`)
- [x] 2.14 `ratatoskr-mobile`: correct nonexistent product-gate, no-migration, and planned workspace integration wording while preserving shared `.omc/`; run both strict OpenSpec checks and available invariant checks, commit to `main`, push, wait for workflows, and record the remote SHA (`4e1d2563590dcadca8b346759a376e7e0b0cd9b8`)
- [x] 2.15 `ratatoskr-browser-extension`: correct nonexistent product-gate and planned workspace integration wording while preserving shared `.omc/`; run both strict OpenSpec checks and available invariant checks, commit to `main`, push, wait for workflows, and record the remote SHA (`79ba2edd64d279e23f640c175ce2fafca2381f39`)
- [x] 2.16 `ratatoskr-export-agent`: correct nonexistent product-gate and planned workspace integration wording; run both strict OpenSpec checks and available invariant checks, commit to `main`, push, wait for workflows, and record the remote SHA (`9eaca566cbf0278d72988251a9227a5ba49eb3b0`)
- [x] 2.17 `ratatoskr-workspace`: after all children, update the implemented-repository map, current system flow, topology status, quality-gate measurements, documentation index, child SHA ledger, validation and rollback record; run `openspec validate --all --strict`, `openspec validate --archived`, workspace invariant checks, commit to `main`, push, wait for workflows, and record the remote SHA (`99066c661a1c2c7c8cbf71ffd5f7e8cd623929d5`)

## 3. Coordinated verification

- [x] 3.1 Re-read all 17 remote `main` heads, verify each contains its recorded commit, every triggered workflow completed successfully, all task worktrees are clean, all pre-existing shared changes are intact, no secret or local-only path was committed, and the final workspace record names every validation result and remaining risk; this is cross-repository evidence and has no behavioral failing test
